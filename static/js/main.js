// main.js
document.addEventListener('DOMContentLoaded', function() {
    // Инициализация календаря
    initCalendar();
    
    // Инициализация заметок
    initNotes();
    
    // Инициализация туду-листа (уже существующий код)
    initTodo();
});